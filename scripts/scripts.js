"use strict";

{
    // Task 3

    const num = "711117";
    if ((+num[0] + +num[1] + +num[2]) === (+num[3] + +num[4] + +num[5])) {
        console.log ("Да");
    } else {
        console.log ("Нет");
    }

}

{
    // Task 4

    const a = -1;
    const moreAnswer = a > 0 ? "Верно" : "Неверно";
    console.log(moreAnswer);

}